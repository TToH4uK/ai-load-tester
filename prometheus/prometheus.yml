global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'bank-bot-monitoring'
    scrape_interval: 5s
    scrape_timeout: 4s
    static_configs:
      # ВАЖНО: меняем bank-bot на host.docker.internal
      - targets: ['host.docker.internal:8000']